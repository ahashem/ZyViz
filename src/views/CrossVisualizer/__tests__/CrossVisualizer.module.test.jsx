import React from 'react';

// import CrossVisualizer from '../CrossVisualizer';

beforeAll(() => {
});

describe('<CrossVisualizer /> Dashboard', () => {
  test('It should match the snapshot', () => {
  //  TODO: Add snapshot testing - High level
  });
});
